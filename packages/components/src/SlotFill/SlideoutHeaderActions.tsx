import { createSlotFill } from '@wordpress/components';

const { Slot, Fill } = createSlotFill('SlideoutHeaderActions');

export const SlideoutHeaderActionsSlot = Slot;
export const SlideoutHeaderActionsFill = Fill;
